const greeter = (name = 'Vaishali', age) =>{
    console.log(name);
}

greeter('Nilesh'); // Nilesh
greeter(); // vaishali

// const transaction = (type, {label, stock, price}) => {
//     console.log(label, stock, price);
// }

// transaction('order'); // no error
// transaction() // error for label, stock and price 

const transaction = (type, {label, stock, price} = {}) => {
    console.log(label, stock, price);
}
transaction() // No error
